.dart_tool/
.packages
build/
.idea/
.vscode/
*.iml
pubspec.lock
**/*.g.dart
**/*.freezed.dart
lib/l10n/app_localizations*.dart

# Local run/analysis output
run_log.txt
Analyze_logs.txt
# No trailing slash: this was created as a plain file, and `_deltest/` matches
# only a directory, so the rule silently did nothing and the file got committed.
_deltest

# Config & secrets — never commit
env/*.json
!env/*.example
*.jks
*.keystore
key.properties
android/key.properties
service-account*.json
.env
.env.*
!.env.example
