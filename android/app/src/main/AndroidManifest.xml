<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        Bez tohohle řádku nemá release build přístup k síti.

        Flutter šablona přidává INTERNET jen do src/debug a src/profile, kde ho
        potřebuje hot reload. Do src/main ho nedává, protože nepředpokládá, že
        aplikace na síť chodí — a tak na to přijdeš až u prvního APK v ruce.
        Debug na emulátoru jede, prohlížeč na tom samém telefonu tu adresu
        otevře, a aplikace tvrdí:

            Failed host lookup: 'dehgpsnemmemnxbhujai.supabase.co'
            OS Error: No address associated with hostname, errno = 7

        Což zní jako rozbité DNS nebo špatná adresa a je to ani jedno: Android
        bez tohohle oprávnění překlad jména prostě neprovede.

        READ_CALENDAR tady chybět nemusí — přichází z manifestu balíčku
        planto_calendar a merger ho doplní do všech build typů. Proto kalendář
        v release APK fungoval a síť ne.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="PlanTo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Supabase OAuth callback. Must match kOAuthRedirect in
                 auth_repository_impl.dart AND the redirect allowlist in
                 Supabase → Authentication → URL Configuration. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="app.planto" android:host="login-callback"/>
            </intent-filter>

            <!-- Návrat z Google OAuth při připojování kalendáře.
                 Vlastní host, ne login-callback: přihlášení a přístup ke
                 kalendáři jsou dvě různé věci a jeden kód doručený do špatné
                 obrazovky by skončil tichým nic. Google sem nepřesměrovává
                 přímo — vlastní schéma u klienta typu Web application
                 nepovoluje — přeposílá to docs/oauth.html. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="app.planto" android:host="calendar-callback"/>
            </intent-filter>

            <!-- Invite links, currently on GitHub Pages.
                 android:autoVerify is deliberately OFF: verification requires
                 /.well-known/assetlinks.json at the DOMAIN root, and a GitHub
                 project page can only serve files under /PlanTo/. Serving it
                 would need a repo named patrikr9.github.io, or the real
                 domain. Without autoVerify the link still opens the app — via
                 the "open with" chooser instead of directly. Turn it on
                 together with planto.app. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="patrikr9.github.io"
                      android:pathPrefix="/PlanTo/i/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
